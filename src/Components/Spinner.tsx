import React from 'react';

const Spinner = (props: any) => {
  return <div className={`h-8 w-8 animate-spin rounded-full border-4 border-solid border-cyan border-b-gray-200`} />;
};

export default Spinner;
